name: dartx_validate
description: A starting point for Dart libraries or applications.
version: 0.0.1
repository: https://github.com/ItsMeViipeR/dartx_validate
homepage: https://github.com/ItsMeViipeR/dartx_validate

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
